# OpenCode — ASPECTS status

- 2025-10-19: wave "Tag normalization for automation & integrations" - updated 68 aspects (24 `integrations`, 21 `automation`, 23 `architecture`), unified tags before typing `integration/automation/github/providers/mcp/slack/console/cloudflare/billing/email` And `automation/cli/ci/release/debug/runtime/models/distribution/workspace/analytics`, reducing the unique tags in these sections to 10. Updated `lastUpdated` and left the bundles on the previous sources. `tags_manager` showed that catalog `formatting` And `metrics` now without links - candidates for deletion are left after revision of the remaining sections. Checks: `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json`, `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json`, `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json`, `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts`, `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json`. `adi.py` isolated isolates `automation-config-schema`, `automation-root-bunfig`, `automation-opencode-bunfig` - on the next cycle you need to connect them with `cli-config`/`workspace` scripts (tags or additional sources).

- 2025-10-19: wave "Test coverage & SDK internals" - added 50 aspects (12 `quality-tui`, 19 `quality-go-sdk`, 13 `sdk-generated`, 6 auxiliary configurations). New sections created `TUI Test Suites`, `Go SDK Test Suites`, `SDK Generated Internals`; documented `tui/.goreleaser.yml`, `sdk/go` CI, `bunfig.toml`, `desktop/vite.config.ts`, declarations `lang-map`. Removed unused tags `migration`, `monorepo`, `projects`, `search`, `solidstart`, counters were synchronized. Checks: `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json`, `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json`, `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts`, `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json`, `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json`. New Warnings: General `ADIw=38.75`, sections with ADIw > 5 (`automation`, `architecture`, `runtime*`, `platform-utils`, `quality`, `sdk`, `tui-*`, `tool-guides`, `console-*`). There is a need to strengthen connections between `automation`↔`runtime` and unload `tui-foundations` (ADIw=18.07). Sections `desktop` (40) and `sdk` (47) still >25 records - schedule split/aggregation.

- 2025-10-19: carried out a wave of "CSS taxonomy & CI normalization" - updated 62 aspects (14 `ci-workflows`, 47 css modules of the console/design system/share, `console-theme-schema`), added tag `css` and synchronized the directory via `tags_manager.py`. Section `ci-workflows` now uses 9 tags (actions, automation, ci, workflow, release, github, cloudflare, vscode, testing) instead of 13; `console-theme-schema` connected to `design-system`, and the console and share css entries are now linked by a single tag. ADIw: `console-shell` 16.25 → 5.95, `design-system` 10.29 → 6.12, `share-web-styles` 2.90 -> 1.48; overall ADIw rose to 33.69 due to weak ties `automation`, `runtime`, `runtime-tools`, `platform-utils` -- brought them to follow-up. Teams: `python3 -m json.tool --no-ensure-ascii` (ASPECTS + tags), `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts`, `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json`, `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 50 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json`. Next Steps: Normalize Tags `automation` And `runtime*`, strengthen connections `platform-utils`/`tool-guides`, add `plugin-sdk` (<=5 entries) and clear unused tags (`migration`, `monorepo`, `projects`, `search`, `solidstart`).

- 2025-10-19: conducted a wave of "CI workflows & theme catalogs" - added 50 aspects (14 to the new section `ci-workflows`, 11 to the new section `tui-themes`, 10 to the new section `share-web-styles`, 8 test records per section `quality`, 6 console CSS/tokens, 1 entry `data`). The tag catalog has been replenished `accounts`, `design-system`, `provisioning`, `testing`, counters were synchronized. Checks: `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json`, `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json`, `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts`, `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` (new warning: `ci-workflows` uses 13 tags >10), `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` (`ADIw=32.12` >=5; new isolate `console-theme-schema`, also high ADIw `console-admin`, `design-system`, `runtime*`, `platform-utils`). Next steps: link `console-theme-schema` with aspects `design-system`/`tui-themes` (roughly through common tags or slug links), normalize the set of tags into `ci-workflows` (keep the basic ones `actions/ci/automation/release`), continue reducing ADIw for overloaded sections.

- 2025-10-19: carried out the "desktop/ui + SDK internals" wave - added 50 aspects (10 for `desktop` UI/utilities, 4 for `design-system`, 9 marketing components `console-site`, 1 for `share-web`, 3 TUI clipboard/input documents, 4 JS SDK scripts, 15 internal Go SDK modules, 3 VS Code SDK configurations, 2 npm pre/postinstall scripts). `desktop` grew to 39 records (>25), `sdk` up to 47 (>25), `design-system` up to 20, `platform-utils` until 16. Launched `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json`, `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts` (the directory still complains about missing counters `accounts`, `design-system`, `provisioning`, `tools`), `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` (old warnings: large sections use >10 tags, `desktop`/`sdk` exceed 25 records), `python3 aspects/scripts/adi.py ...` (`ADIw=39.42` >=5; insulators have been preserved in `console-workspace/*`, styles `console-admin/*`, `console-shell-*`). Next steps: group workspace/admin styles under common tags and link to existing routes, consider splitting `desktop` on the UI/utility subsection or add cross tags to the TUI/CLI, update the catalog of tags with missing counters.
- 2025-10-16: designed the "console layout & docs" wave - added 50 aspects (12 for `console-shell` CSS/palettes, 6 for `console-communications` assets, 7 for `console-admin` CSS, 4 for `console-billing`, 3 for routes `console-routing`, 1 for `plugin-sdk` publish, 11 articles `share-web` and 6 desktop components). `console-shell` grew to 15 records, `console-admin` until 13, `console-billing` until 11, `console-communications` until 9, `share-web` until 21, `desktop` up to 29. Synchronized tags (`tags_manager.py sync-counts`). Checks: `python3 -m json.tool --no-ensure-ascii` (ASPECTS + tags), `audit.py` (warnings: `desktop` >25 entries, most historical sections still >10 tags), `adi.py --ignore-tag-frac 0.15` (ADIw=62.73; isolated `console-workspace/*`, new CSS aspects `console-admin/*`, `console-shell-*` And `app-*`; sections with high ADIw: `console-workspace` (inf), `console-admin` (94.22), `console-shell` (25.44), `applications`, `cli`, `runtime-tools`, `tui-app`, `tui-foundations`). Next steps: combine workspace/admin styles under a common tag (eg. `workspace-ui`), bind CSS aspects to existing components via slug references/tags `workspace`, think about crushing `desktop` (UI vs utilities) and link `console-workspace` records with routing/shell aspects to reduce ADIw.

- 2025-10-17: console aspects were repartitioned - auth/billing/workspace routes were moved to new sections `console-auth`, `console-billing`, `console-workspace`, `console-routing`, `console-shell`, `console-communications`, added a web viewer `share-web` (10 entries) and catalog `plugin-sdk` (4 records), moved 22 previous records from `console-admin` and added 30 new aspects (routes `/auth`, 404/Discord/docs proxy, email templates, share viewer, plugin SDK, build/publish/schema scripts). Entered tags `routing`, `share`, `web`, tag counters were synchronized. `console-admin` shrunk to 6 entries (utilities only), new sections received <=4 unique tags. Checks: `python3 -m json.tool --no-ensure-ascii` (ASPECTS and tags), `python3 aspects/scripts/tags_manager.py --tags-path ... --aspects-path ... sync-counts`, `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` (still warnings about >10 tags in large sections and small `console-*` catalogs), `python3 aspects/scripts/adi.py ... --tags-catalog ...` (ADIw=47.93, isolated `provider-models-macro` V `architecture`, `console-shell-legal-footer` And `tui-util-apilogger`). The next step is to link the shell/legal aspect to the rest of the pages and lower the ADIw `cli`/`runtime-tools` due to common tags.
- 2025-10-17: created a section `runtime-foundations` (migration of core Instance/State and five runtime layers) and `console-site` for landing/marketing, added 30 new aspects for util/runtime/terminal/console and updated 20 existing ones (50 records in total with `lastUpdated=2025-10-17`). Marketing Solid components moved from `applications` V `console-site`, and session schema/lock/file guard - in `runtime-foundations`. Removed duplicates `util-*` from `runtime-tools`, expanded CLI template tags (`runtime`) and platform utilities (`runtime/tooling`) and synchronized the tag directory. Strengthened the connectivity of TUI and CLI: introduced tags `terminal-app`/`terminal-input`/`terminal-clipboard`/`terminal-render`, tagged 72 entries, added cross-links inside `cli-commands`, `runtime/server-core`, terminal clipboard and `console-site`. Additionally, we created bridging tags (`terminal-bridge`, `cli-templates`, `prompt-builder`, `tui-shell-ui`, `automation-diagnostics`, `lsp-backend`, `workflow-config`, `model-catalog`, `tui-logging`) and applied them to the connecting CLI/TUI, automation and console aspects. Final checks: `python3 -m json.tool --no-ensure-ascii` (ASPECTS and tags), `tags_manager.py sync-counts`, `audit.py` (warnings >10 tags and volume `console-admin`/`tui-app`/`cli-*`), `adi.py` (ADIw=44.82, isolates=0; sections with ADIw>5: `applications`, `architecture`, `automation`, `cli`, `cli-commands`, `console-admin`, `design-system`, `platform-utils`, `runtime`, `runtime-foundations`, `runtime-tools`, `terminal-ui`, `tool-guides`, `tui-app`, `tui-foundations`). Next steps: optimize tag sets of overloaded sections and prepare a crushing plan `console-admin`.
- 2025-10-16: sections added `session-prompts`, `tool-guides`, `tui-app` (53 new aspects: provider prompt scripts, tool instructions and Go TUI core), recalculated tags (`python3 aspects/scripts/tags_manager.py --tags-path ... sync-counts`). Plan: Link isolated CLI entries (`cli-bootstrap`, `cli-command-wrapper`, `cli-serve`, `cli-global-path`, `cli-ide-detector`, `cli-models-provider`) to runtime/automation aspects, unify prompt/tool tags and prepare splitting `tui-app` into subsections (state/ui/infra).
- 2025-10-16: Go TUI coverage expanded (new sections `tui-components` And `tui-foundations`, +30 aspects for lists, dialogs, diff, layout/theme/util). You need to merge them with existing ones `tui-app` tags, otherwise `tui-app`/`tui-components`/`tui-foundations` are now completely isolated.
- 2025-10-16: sections added `design-system` And `terminal-ui` (31 new aspects of the CSS/JSX library and Go TUI input pipeline), TUI commands were moved from `cli-commands`, normalized CLI command tags (33->7 unique tags) and uploaded five `.opencode/*` hints. `python3 aspects/scripts/tags_manager.py --tags-path ... sync-counts` And `python3 -m json.tool --no-ensure-ascii` (for both JSON) integrity verified, `audit.py` I left only warnings about overloaded sections (architecture/infrastructure/integrations, etc.). `adi.py` (with trimmed `--include-*` due to timeout) showed ADIw=38.94 and isolates (`cli-bootstrap`, `cli-command-wrapper`, `cli-serve`, `console-admin-*`, `workspace-*`, `server-core`, `share-sync-client`), you need to tie them to runtime/automation aspects and group console/workspace blocks.

- 2025-10-16: added 50 aspects for the CLI debug package, LSP autoloaders, Cloudflare share API and workspace UI. Tags replenished (`search`, `projects`, `security`, `websocket`), grew up `cli` (7→8), `applications` (8->21) and `infrastructure` (7->14). Next cycle: unify CLI/Application tags (group debug/analytics), aggregate console sections and transfer share/CLI aspects into common connections, otherwise ADIw and excess of unique tags remain high.
- 2025-10-16: large sections were broken (`cli-commands`, `runtime-tools`, `console-admin`, `platform-utils`, `lsp`), added 41 new aspects for console billing, Zen gateway, backend scripts and desktop components. `audit.py` still warns about >10 tags in most sections - tag unification is needed (especially for CLI/desktop/automation). `adi.py` (ADIw=22.42) showed isolates `cli-bootstrap`, `cli-command-wrapper`, `cli-github-agent`, `cli-upgrade-channel`, `core-lsp-server`, `server-core`, `server-tui-bridge`; in the next iteration, connect them with tags/descriptions to runtime and automation.

- 2025-10-16: added 30 aspects (CLI bootstrap/export/serve/tui, server core/routes, tool LSP, util-* helpers). `architecture`=36, `runtime`=37, `cli`=19 entries - requires splitting into subsets (core vs util, session vs tooling, cli-shell vs cli-ui) and reusing existing tags instead of splitting.
- 2025-10-16: sections expanded `architecture`, `runtime`, `sdk` (50 new aspects on core modules, session tools and Go/JS SDK). `architecture` has grown to 27 entries - refactoring into subsets (for example, core-platform/tooling) and unification of tags is planned in the next revision.
- 2025-10-15: a basic set of sections (architecture, infrastructure, cli) and a tag dictionary have been created; the next iteration expands to cover grocery bags.
- 2025-10-15: records expanded (infra/auth, CLI GitHub, runtime sessions), section added `runtime`, tag catalog updated and aspects linked through slug mentions.
- 2025-10-16: 50 aspects added for new sections (`applications`, `desktop`, `automation`, `integrations`, `data`, `sdk`, `observability`), replenished `architecture`/`cli`/`runtime`, console/desktop entries from infrastructure have been moved, the tag dictionary has been expanded.

## 2025-10-16 Checks (session/tool/TUI refresh)
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings for >10 unique tags (architecture/cli/runtime/... + new `tui-app`). Requires unification of tags to group bins before splitting.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` *(option without `--include-*` due to timeout)* -- ADIw=29.85 (>5). Insulators: `cli-bootstrap`, `cli-command-wrapper`, `cli-serve`, plus in section `cli` (`cli-global-path`, `cli-ide-detector`, `cli-models-provider`) And `runtime` (`server-core`, `share-sync-client`). Next steps: add cross-tags `automation/runtime`, include links to new tool guides, prepare general descriptions of the CLI lifecycle.

## 2025-10-16 Checks (TUI components/foundations batch)
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/tags_manager.py --tags-path aspects/projects/OpenCode/ASPECTS.tags.json --aspects-path aspects/projects/OpenCode/ASPECTS.json sync-counts`
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- constant warnings about >10 unique tags, plus new sections `tui-components`/`tui-foundations`/`tui-app` added a share of UI tags; need to link them to `runtime`/`tool-guides` and crush if necessary.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` -- ADIw=42.67 (>5). Isolated 44 entries, sections `tui-app`, `tui-components`, `tui-foundations`, `terminal-ui` completely without connections. Required: 
  - add shared tags/descriptions between CLI/TUI (for example `cli-tui`, `server-tui-bridge`, `tui-app-entrypoint` ↔ `runtime`/`automation`);
  - include tool-guides in component descriptions (mutual slug links);
  - divide TUI into subgroups (for example `tui-app-core`, `tui-ui-components`, `tui-rendering`) and link to runtime/cli sections.

## 2025-10-16 Checks (CLI/LSP/Infra additions)
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings: all main sections (>10 unique tags). Plan: collect a dictionary of tags for CLI/console/application and reduce duplicate tags before splitting large sections.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --adi-threshold 5.0 --show-isolates --isolate-limit 5 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` *(full set with `--include-source-links` timed out, so we used a reduced limit of isolates)* - ADIw=35.22 (>5). Critical sections: `cli` (isolated: `cli-command-catalog`, `cli-global-path`, `cli-ide-detector`, `cli-models-provider`), `cli-commands` (isolated: `cli-bootstrap`, `cli-github-agent`, `cli-upgrade-channel`), `console-admin` (isolated: key/member/model/provider/usage sections), `applications` (isolated: `workspace-keys`, `workspace-members`), `runtime` (isolated: `server-core`, `server-tui-bridge`, `share-sync-client`). Plan: link new CLI/console aspects with common tags (`runtime`, `analytics`, `automation`), add cross API/console descriptions and merge duplicate UI aspects.

## 2025-10-16 Checks (CLI/server/util expansion)
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings: `architecture`=36 (>25), `runtime`=37 (>25), most sections exceed the unique tag limit. Plan: smash `architecture` (core platform vs util) and `runtime` (session/state vs tools) into separate sections, unify CLI tags (bootstrap/UI/commands) to common ones (`cli`, `tooling`, `runtime`).
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` -- ADIw=24.91 (>5). Insulators: `cli-bootstrap`, `server-core`, `server-tui-bridge`, `sdk-stainless-generate`, `sdk-vscode-extension`. Plan: Strengthen communication through additional tags (`automation`, `api`, `runtime`), add aspects/descriptions of the server and SDK pipeline with cross-references and highlight thematic sections.

## 2025-10-16 Checks (architecture/runtime/sdk expansion)
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings: `architecture` contains 27 entries (>25) and 22 unique tags; most sections exceeded the unique tag threshold. Plan: identify subsets within architecture (core-platform vs tooling), revise CLI/runtime tags and aggregate overlapping labels in the next cycle.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` -- ADIw=23.61 (>5). Critical sections: `architecture` (ADIw=12.23), `automation` (isolated `automation-debug-suite`, `automation-snapshot-tools`), `cli` (6.75), `runtime` (7.55), `sdk` (isolated `sdk-stainless-generate`, `sdk-vscode-extension`). Plan: connect automation scenarios with new runtime aspects, add reference entries (CLI and automation) to the SDK and prepare to split the architecture into thematic blocks along with reducing tags.

## 2025-10-16 Checks
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings: a large number of unique tags in `architecture`, `cli`, `integrations`, `runtime`, single/small sections (`observability`, `automation`, `desktop`, `data`, `applications`). Plan: Combine tags around common axes (e.g. `cli` And `runtime` bind to `automation`), add `observability` and small sections with new interconnected entries.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` -- ADIw=12.19 (>5). There are no isolates; excesses remained in `architecture` (5.57) and `cli` (6.70). Plan: further reduce unique CLI tags and link architectural entries with operational aspects (runtime/automation) in the next cycle.

## 2025-10-15 Checks
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.json` — OK
- `python3 -m json.tool --no-ensure-ascii aspects/projects/OpenCode/ASPECTS.tags.json` — OK
- `python3 aspects/scripts/audit.py aspects/projects/OpenCode/ASPECTS.json` -- warnings: small sections (architecture=1, cli=4, runtime=2) and 14 unique tags in infrastructure; plan: expand the architecture and unify the infra tags in the next revisions.
- `python3 aspects/scripts/adi.py aspects/projects/OpenCode/ASPECTS.json --per-section --weighted --ignore-tag-frac 0.15 --include-slug-links --include-source-links --adi-threshold 5.0 --show-isolates --isolate-limit 20 --tags-catalog aspects/projects/OpenCode/ASPECTS.tags.json` — ADIw=6.01 (>5), isolate: `monorepo-structure`; follow-up: unpack the architectural section and link it to infra/cli aspects.
